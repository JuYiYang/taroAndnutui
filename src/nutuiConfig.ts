import { Button, Toast, Swiper, SwiperItem, ImagePreview, Cell, Divider} from '@nutui/nutui-taro';

type listArray = any

const list: listArray = [
  Button,
  Toast,
  Swiper,
  SwiperItem,
  ImagePreview,
  Cell,
  Divider
]

export default list